server {
  listen 8080;
  server_name _;

  # Envoy rewrites /vite/* → /* before requests reach this container,
  # so Nginx only ever sees paths without the /vite prefix.
  root /usr/share/nginx/html/vite;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
